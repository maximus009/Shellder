# At times when even auto complete on your
# shell/terminal is not enough and
# you are high on gitting

# Add the following lines to your
# bash profile or bashrc 
# and git faster than before

alias Push='git push origin $1'
alias Status='git status'
alias Pull='git pull $1'
